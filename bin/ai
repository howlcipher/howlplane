#!/usr/bin/env bash
# Deprecated compatibility launcher for the HowlPlane control plane.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" 2>/dev/null && pwd || true)"
export HOWLPLANE_LEGACY_AI=1
exec "${SCRIPT_DIR}/howlplane" "$@"
